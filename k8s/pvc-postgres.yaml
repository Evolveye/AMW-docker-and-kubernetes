apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: projekt-pvc-postgres
spec:
  storageClassName: managed-nfs-storage
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
